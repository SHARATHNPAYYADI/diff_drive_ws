ekf_filter_node:
  ros__parameters:
    # Process noise (Q) – small to trust odom prediction
    process_noise: [0.01, 0.01, 0.01]

    # Measurement noise for scan_pose – higher, so odom dominates
    scan_pose_covariance: [1.0, 1.0, 0.5]
